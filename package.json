{
  "name": "gist-alumni-system",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "client:build": "npm --prefix client run build",
    "client:dev": "npm --prefix client run dev",
    "server:dev": "npm --prefix server run start"
  }
}
